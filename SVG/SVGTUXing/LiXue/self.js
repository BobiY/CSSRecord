// 合力计算
var f = Math.sqrt(Math.pow(f1) + Math.pow(f2))

// 计算加速度
const m = 1;
var a = f/m;

// 速度
const v0 = 0;
const t = 1/200;
var v = at;

// 位移
const xishu = 0.03
var s = f/xishu;

// 两个点之间的向量

x1 = 0;
x2 = 0;
y1 = 0;
y2 = 0;

const dx = x2 - x1;
const dy = y2 - y1;

/**
 * 1. 计算出合力
 * 2. 计算出加速度
 * 3. 计算出速度
 * 4. 计算出在规定事件内的位移
 */